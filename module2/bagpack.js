class Backpack {
    constructor(volume,color)
    {
        this.volume=volume;
        this.color=color;
    }

    carryLuggage(color){
        this.color=color;
    }
}
const onj=new Backpack(20,'red')
console.log(onj)
let city=new String("bangalore")
console.log(city)
